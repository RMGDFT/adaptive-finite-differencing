Output files for QE and each RMG finite difference operator are stored in the corresponding subdirectory. Files are labeled from the lowest to the highest cutoff input1-input7. Command line (sed) was used to generate the actual input files used in the run from the templates in the top level directory.
